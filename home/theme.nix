{ config, pkgs, lib, userConfig, ... }:

{
  # Dunst config is managed by stow — see dotfiles/dunst/
  # Fuzzel config is managed by stow — see dotfiles/fuzzel/
  # Swaylock config is managed by stow — see dotfiles/swaylock/
}
